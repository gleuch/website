require 'rails_helper'

feature "Responsive" do

  describe "should be valid" do
    scenario "for desktop"
    scenario "for tablet"
    scenario "for mobile"# do
    #   visit static_page_path(:about)
    #   expect(page).to have_text("2003–#{Date.today.year} Greg Leuch, unless noted differently")
    #   expect(page).not_to have_selector('#latest-activity', visible: true)
    # end
  end

end